import React from 'react'
import {Text, View, Button, TouchableOpacity,Image, StyleSheet} from 'react-native'

export const FavouriteMovies = ({navigation, route}) => {
    return (
        <View>
            
           
        </View>
    )
}